<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container px-lg-5">
                <a class="navbar-brand" href="/">Memo URL</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        <li class="nav-item"><a class="nav-link <%= page_name==='index' && 'active'%>" aria-current="page" href="/">Home</a></li>
                        
                        <% if (isAuth) { %>
                            <li class="nav-item"><a class="nav-link <%= page_name==='dashboard' && 'active'%>" href="/user/dashboard">Dashboard</a></li>
                            <li class="nav-item"><a class="nav-link <%= page_name==='logout' && 'active'%>" href="/user/logout">Logout</a></li>
                        <% } %>
                        <% if (!isAuth) { %>
                            <li class="nav-item"><a class="nav-link <%= page_name==='login' && 'active'%>" href="/login">Login</a></li>
                            <li class="nav-item"><a class="nav-link <%= page_name==='register' && 'active'%>" href="/register">Register</a></li>
                        <% } %>

                    </ul>
                </div>
            </div>
</nav>